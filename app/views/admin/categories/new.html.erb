<h1>New Category</h1>

<%= form_with(model: [:admin, @category], url: admin_categories_path, method: :post) do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit 'Create Category' %>
  </div>
<% end %>